apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: awx-data
  namespace: awx
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
